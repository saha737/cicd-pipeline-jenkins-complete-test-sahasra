plugins {
//inlude the nodeJS plugin to execute nodejs and npm tasks
  
  id "com.github.node-gradle.node" version "7.0.2"

}


node {
    version = '18.18.2'         // stable Node.js version
    npmVersion = '9.8.1'        // compatible NPM version
    download = true
}



//declare a build task
task build
task clean(type: Delete) {
    delete fileTree(dir: '.', include: ['dist/**', 'node_modules/**'])
}

//declare a task to create a zip of the app
task zip(type: Zip) {
from ('.') {
include "*"
include "bin/**"
include "data/**"
include "node_modules/**"
include "public/**"
include "routes/**"
include "views/**"
}
destinationDirectory = file('dist')
archiveBaseName.set("trainSchedule")
}

//declare task dependencies
build.dependsOn zip
zip.dependsOn npm_build
npm_build.dependsOn npm_test
npm_test.dependsOn npmInstall